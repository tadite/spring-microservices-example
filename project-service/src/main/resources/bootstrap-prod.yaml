spring:
  cloud:
    config:
      uri: http://config-server:8888
      password: 1234
      username: user
      fail-fast: true
      retry:
        initial-interval: 1500
        multiplier: 1.5
        max-attempts: 10000
        max-interval: 1000